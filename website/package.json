{
  "scripts": {
    "now-build": "vuepress build ../docs --dest ./dist && now && now alias",
    "deploy": "vuepress build ../docs --dest ./dist && now && now alias"
  },
  "devDependencies": {
    "now": "^13.1.3",
    "vuepress": "^0.14.8"
  }
}
