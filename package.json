{
  "private": true,
  "scripts": {
    "build": "rm -rf ./website/yarn.lock && cd ./website && npm i && npm run deploy && cd -",
    "doc:deploy": "rm -rf ./website/yarn.lock && cd ./website && npm run deploy && npm i && cd -",
    "doc:dev": "./website/node_modules/.bin/vuepress dev ./docs"
  }
}
